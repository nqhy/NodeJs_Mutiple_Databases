import { RequestMethod } from './CommonTypes';

export const defaultAllowRoutes: Array<string> = [
  '/api/v1/signup',
  '/api/v1/login',
];

export const defaultAllowMethods: Array<RequestMethod> = ['GET'];
